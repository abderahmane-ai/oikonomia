"""Model-release infrastructure: the licence firewall that gates every Hub push.

Kept deliberately dependency-free (no torch, no Modal) so the release path can be
audited and tested on a laptop.
"""
